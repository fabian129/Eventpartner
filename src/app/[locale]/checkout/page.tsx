import { CheckoutContent } from "./CheckoutContent";

export const metadata = {
  title: "Checkout | EventPartner",
  description: "Complete your merchandise order",
};

export default function CheckoutPage() {
  return <CheckoutContent />;
}
